- getAnswer é importante
    - retorna string?
        - se for list - é so uma resposta
        - se for confirm ou outro, que envolvem outras respostas, fica ruim

        - isso pode retornar uma linkedList, com todas as resposta na ordem do usuário
        - se for uma Grande aplicação isso pode ser ruim
        - pode retornar um Map, dependendo, mas como list é somente 1 resposta, isso dificultaria o retorno dela.

        se for linkedList, faz sentido que a interface precise desses métodos

        anwser(string question) {
            se for do tipo list {
                todas as perguntas do addquestion bastaria pra resposta
                se for null, também retorna a resposta
                mas isso nao é contra-intuitivo?
                o cara deveria ler a documentação também né
            }

            se for de outro tipo {
                o parametro é importante
                a resposta da pergunta é revelada usando map.get()
            }
       }

       posso fazer dois tipos de anwser() {
            anwser(string question)

            e anwser() ou answerIfOption()
            se alguem chamar esse método daria erro se não fosse do tipo list. o mesmo poderia ser pra anwser()
       }